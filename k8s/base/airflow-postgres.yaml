apiVersion: apps/v1
kind: StatefulSet
metadata: { name: airflow-postgres }
spec:
  serviceName: airflow-postgres
  replicas: 1
  selector: { matchLabels: { app: airflow-postgres } }
  template:
    metadata: { labels: { app: airflow-postgres } }
    spec:
      containers:
      - name: db
        image: postgres:14
        envFrom: [ { secretRef: { name: pg-airflow-secrets } } ]
        ports: [ { containerPort: 5432, name: pg } ]
        volumeMounts: [ { name: data, mountPath: /var/lib/postgresql/data } ]
  volumeClaimTemplates:
  - metadata: { name: data }
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources: { requests: { storage: 2Gi } }
---
apiVersion: v1
kind: Service
metadata: { name: airflow-postgres }
spec:
  selector: { app: airflow-postgres }
  ports: [ { port: 5432, targetPort: pg } ]
